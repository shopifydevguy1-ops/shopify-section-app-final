---
deployment:
  tasks:
    - export DEPLOYPATH=/home/azwywnto/repositories/shopify-section-app-final
    - cd $DEPLOYPATH
    - npm install --production=false
    - npx prisma generate
    - npm run build
